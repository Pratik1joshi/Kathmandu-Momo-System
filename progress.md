# Progress

## Session
- Phase 1–4 completed for POS images, bills active, orders NPT+poll, customer profile, AR + dashboard + cash flow.
- `node scripts/check-accounting.mjs` passed.

## Remaining (spec stretch — not blocking)
- Full PDF/Excel export on every accounting page
- Multi-branch filters (schema has no branch_id everywhere)
- Bank reconciliation polish, write-off UI, approval workflows
